"""
Tests for the old top-level `pipelines/` scripts have been removed.

This library repo intentionally omits the orchestration scripts; ingestion and
feature engineering are now exercised via unit tests on the underlying
functions in `trading_crab_lib.ingestion` and `trading_crab_lib.transforms`.
"""
